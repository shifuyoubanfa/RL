# 复制成 .env 并填上自己的值。.env 已在 .gitignore 里，不会入库。
# 本文件只放占位符，任何真实 key 都不许写进来。

# 裁判模型的 API key。判分和改写都走它。
# 变量名由 configs/train.yaml 的 judge.api_key_env 决定，改了那里就要改这里。
DASHSCOPE_API_KEY=your-api-key-here

# ---- 下面都是可选的：不设就用 configs/train.yaml 里的值 ----

# 产物根目录。output / ckpts / models / logs 四个子目录挂在它下面
# RL_WORK_DIR=/data/rl-runs

# 被强化的基座权重目录，必须含 config.json
# RL_BASE_MODEL=/models/base-32b

# 原始语料，每行 {query, user_prompt}，格式见 examples/sample_data.jsonl
# RL_RAW_CORPUS=/data/raw_corpus.jsonl

# 训练用哪几张卡；张数即分布式进程数
# RL_TRAIN_GPUS=0,1,2,3,4,5,6,7

# 推理服务用哪几张卡；张数即张量并行度
# RL_VLLM_GPUS=0,1

# 裁判调用的花费围栏（元）。0 = 只记账不设上限
# RL_JUDGE_BUDGET_YUAN=0

# 可执行文件路径。装在不同 conda 环境里时分别指过去
# PYTHON_BIN=/opt/conda/envs/rl/bin/python
# SWIFT_BIN=/opt/conda/envs/rl/bin/swift
# VLLM_BIN=/opt/conda/envs/serving/bin/vllm
